import React from 'react';

export const Header = () =>{
    return(

        <div className="header">
            <nav className="top">
                It's a Top Nav
            </nav>
        </div>

    );
}